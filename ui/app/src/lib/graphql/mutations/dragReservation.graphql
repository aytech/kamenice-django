mutation dragReservation($data: ReservationDragInput!) {
  dragReservation(data: $data) {
    reservation {
      fromDate
      id
      suite {
        id
        number
        title
      }
      toDate
    }
  }
}
